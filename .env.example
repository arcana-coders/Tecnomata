# Rename to .env and fill with your Hostinger / SMTP credentials
# These values are used by the email API route at /api/contact
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=hola@tecnomata.com
SMTP_PASS=REEMPLAZA_CON_CONTRASENA
# If your provider uses STARTTLS on 587 instead of implicit TLS 465, set PORT=587 and SMTP_SECURE=false
SMTP_SECURE=true
# Override default recipient (defaults to SMTP_USER if empty)
CONTACT_TO=hola@tecnomata.com
