{
  "name": "newweb",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "validate-urls": "node scripts/validate-urls.js",
    "check-sitemap": "echo 'Testing sitemap...' && curl -I http://localhost:4322/sitemap-index.xml",
    "seo-check": "npm run build && npm run validate-urls"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.13.4"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "tailwindcss": "^3.4.17"
  }
}
